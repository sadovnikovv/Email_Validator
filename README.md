# Email_Validator (MX + SMTP RCPT) — многопоточная проверка email

Этот проект проверяет email-адреса в несколько этапов:
1) Проверка синтаксиса (библиотека `email-validator`)
2) Проверка домена по DNS (MX-записи)
3) SMTP-проверка доступности получателя (SMTP диалог до `RCPT TO`)

Важно: SMTP-проверка **не даёт 100% гарантии существования ящика** для всех доменов, потому что часть серверов использует catch-all/антиверификацию/временные ответы. [code_file:29]
Статус `valid` в этом проекте означает: “хотя бы один MX вернул 250 на RCPT TO и не было явного permanent-отказа, который однозначно запрещает доставку”. [code_file:29]

## Что есть в проекте

- `smtp_email_checker.py` — основной скрипт (многопоточность, DNS+SMTP, crash-safe сохранение, дружественная финализация). [code_file:29]
- `filters.py` — фильтр “заведомо нецелевых/служебных” адресов (no-reply, postmaster, abuse и т.д.). [code_file:29]
- `filters_config.json` — конфиг фильтров (можно добавлять свои запрещённые local-part до `@`, например `hr`). [code_file:29]
- `rebuild_reports.py` — пересборка Excel-отчётов после падения по `checkpoint.jsonl`/CSV. [code_file:29]
- `requirements.txt` — зависимости. [code_file:29]

## Установка

Требования: Python 3.9+. [code_file:1]

```bash
pip install -r requirements.txt
```


## Входной файл (Excel или CSV)

`INPUT_FILE` может быть:

- Excel: `.xlsx` / `.xls` [code_file:29]
- CSV: `.csv` [code_file:29]


### Заголовок может быть любым или его может не быть

- Если в файле есть заголовок — он может быть любым (например: `Email`, `Значение`, `Почта`, `Contact` и т.п.). [code_file:29]
- Если заголовка нет — первая строка может быть email. [code_file:29]

Программа не привязана к названию колонки: она автоматически ищет колонку, где чаще всего встречаются значения, похожие на email (по шаблону с символом `@`). [code_file:29]

### Разделитель CSV

CSV читается потоково и пытается автоматически определить разделитель (`,`, `;`, таб, `|`). [code_file:29]

## Запуск

Важно: не называйте основной файл `email_validator.py`, чтобы не конфликтовать с пакетом `email-validator`. [code_file:29]

```bash
python smtp_email_checker.py
```


## Результаты (crash-safe)

После запуска создаётся папка:

`results/<YYYY-mm-dd_HH-MM-SS>/` [code_file:29]

Файлы, которые пишутся **во время работы** (append), поэтому при падении прогресс не теряется:

- `checkpoint.jsonl` — 1 строка = 1 результат проверки (удобно для восстановления/аналитики). [code_file:29]
- `valid.csv`, `invalid.csv`, `temporary.csv`, `unknown.csv`, `error.csv` — адреса, разнесённые по статусам. [code_file:29]
- `checked.txt` — список уже обработанных email. [code_file:29]


## Excel-отчёты (опционально)

Создание `.xlsx` может занимать много времени на больших базах, поэтому по умолчанию в коде:

- `BUILD_EXCEL_FILES = False` (Excel отключён). [code_file:29]

Если нужно получить Excel:

- поставьте `BUILD_EXCEL_FILES = True`. [code_file:29]

Также доступна пересборка Excel отдельно после падения/после работы:

```bash
python rebuild_reports.py results/<timestamp>
```


## Фильтрация “нецелевых” адресов

Фильтр позволяет отбрасывать “служебные” ящики и любые адреса по вашим правилам. [code_file:29]
Настройка делается через `filters_config.json`, например: [code_file:29]

- `user_block_local_exact`: `[ "hr", "accounting", "ceo" ]` [code_file:29]


## Настройка производительности

Параметры вверху `smtp_email_checker.py`:

- `NUM_THREADS` — число потоков (скорость vs риск блокировок). [code_file:29]
- `DNS_TIMEOUT_SEC`, `SMTP_TIMEOUT_SEC` — таймауты. [code_file:29]
- `MAX_MX_SERVERS` — сколько MX проверять. [code_file:29]

Рекомендация для очень больших баз:

- оставлять `BUILD_EXCEL_FILES=False` и работать по CSV/JSONL. [code_file:29]


## Правовая заметка

Проверяйте только базы, на обработку которых есть законные основания/согласие.